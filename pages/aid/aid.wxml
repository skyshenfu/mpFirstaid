<view class="aidwhole">
<block wx:for="{{aidtypes}}" wx:for-item="type" wx:key="*this">
<view class="aidtype">
  <image src="{{type.typesrc}}" style="width: 140rpx; height: 140rpx;"></image>
  <text class="aidtypetext">{{type.typetext}}</text>
  </view>
</block>
</view>